import matplotlib.pyplot as plt\nimport numpy as np\n\ndef plot_predictions(predictions, labels):\n    plt.figure(figsize=(10, 5))\n    x = np.arange(len(predictions))\n    plt.bar(x, predictions, tick_label=labels)\n    plt.xlabel('Predictions')\n    plt.ylabel('Probability')\n    plt.title('Predicted Outcomes')\n    plt.show()\n\ndef plot_multiple_predictions(prediction_sets, labels):\n    plt.figure(figsize=(10, 5))\n    x = np.arange(len(labels))\n    for i, predictions in enumerate(prediction_sets):\n        plt.plot(x, predictions, marker='o', label=f'Set {i + 1}')\n    plt.xlabel('Labels')\n    plt.ylabel('Probability')\n    plt.title('Multiple Prediction Sets')\n    plt.legend()\n    plt.show()